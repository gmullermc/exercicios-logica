#Lista 3. Faça um Programa que leia 20 números inteiros e armazene-os num vetor. Armazene os
#números pares no vetor PAR e os números IMPARES no vetor impar. Imprima os três
#vetores

numeros = []
impares = []
pares = []
n = 6
for i in range (0, n):
  print('Insira um numero: ')
  a = int(input())
  numeros.append(a)

x = numeros[0]

for x in range(0, len(numeros)):
  if x%2 == 0:
    pares.append(x)
  else:
    impares.append(x)

  print(numeros)
  print(pares)
  print(impares)